/*
 * tachyon-field-api
 *
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.25
 *
 * Generated by: https://openapi-generator.tech
 */

use super::{configuration, ContentType, Error};
use crate::{apis::ResponseContent, models};
use reqwest;
use serde::{de::Error as _, Deserialize, Serialize};
use tokio::fs::File as TokioFile;
use tokio_util::codec::{BytesCodec, FramedRead};

/// struct for typed errors of method [`analyze_golf_course_tabular`]
#[derive(Debug, Clone, Serialize, Deserialize)]
#[serde(untagged)]
pub enum AnalyzeGolfCourseTabularError {
    Status400(models::ErrorResponse),
    UnknownValue(serde_json::Value),
}

/// Deprecated. Use POST /v1/bridge/tabular/analyze, which runs the same analyzer and requires field:PreviewBridgeRun. This alias exists only so CourseBoard can roll back to it, and is not removed because /v1/ endpoints are not deleted in place.
pub async fn analyze_golf_course_tabular(
    configuration: &configuration::Configuration,
    file: std::path::PathBuf,
    context: Option<&str>,
    header_row: Option<i32>,
    idempotency_key: Option<&str>,
    mapping_mode: Option<&str>,
    mapping_proposal: Option<&str>,
    rotation: Option<&str>,
    sheet_name: Option<&str>,
    target_schema: Option<&str>,
) -> Result<models::TabularAnalyzeResponse, Error<AnalyzeGolfCourseTabularError>> {
    // add a prefix to parameters to efficiently prevent name collisions
    let p_form_file = file;
    let p_form_context = context;
    let p_form_header_row = header_row;
    let p_form_idempotency_key = idempotency_key;
    let p_form_mapping_mode = mapping_mode;
    let p_form_mapping_proposal = mapping_proposal;
    let p_form_rotation = rotation;
    let p_form_sheet_name = sheet_name;
    let p_form_target_schema = target_schema;

    let uri_str = format!(
        "{}/v1/erp/extensions/golf-course/tabular/analyze",
        configuration.base_path
    );
    let mut req_builder = configuration
        .client
        .request(reqwest::Method::POST, &uri_str);

    if let Some(ref user_agent) = configuration.user_agent {
        req_builder = req_builder.header(reqwest::header::USER_AGENT, user_agent.clone());
    }
    if let Some(ref token) = configuration.bearer_access_token {
        req_builder = req_builder.bearer_auth(token.to_owned());
    };
    let mut multipart_form = reqwest::multipart::Form::new();
    if let Some(param_value) = p_form_context {
        multipart_form = multipart_form.text("context", param_value.to_string());
    }
    let file = TokioFile::open(&p_form_file).await?;
    let stream = FramedRead::new(file, BytesCodec::new());
    let file_name = p_form_file
        .file_name()
        .map(|n| n.to_string_lossy().to_string())
        .unwrap_or_default();
    let file_part =
        reqwest::multipart::Part::stream(reqwest::Body::wrap_stream(stream)).file_name(file_name);
    multipart_form = multipart_form.part("file", file_part);
    if let Some(param_value) = p_form_header_row {
        multipart_form = multipart_form.text("headerRow", param_value.to_string());
    }
    if let Some(param_value) = p_form_idempotency_key {
        multipart_form = multipart_form.text("idempotencyKey", param_value.to_string());
    }
    if let Some(param_value) = p_form_mapping_mode {
        multipart_form = multipart_form.text("mappingMode", param_value.to_string());
    }
    if let Some(param_value) = p_form_mapping_proposal {
        multipart_form = multipart_form.text("mappingProposal", param_value.to_string());
    }
    if let Some(param_value) = p_form_rotation {
        multipart_form = multipart_form.text("rotation", param_value.to_string());
    }
    if let Some(param_value) = p_form_sheet_name {
        multipart_form = multipart_form.text("sheetName", param_value.to_string());
    }
    if let Some(param_value) = p_form_target_schema {
        multipart_form = multipart_form.text("targetSchema", param_value.to_string());
    }
    req_builder = req_builder.multipart(multipart_form);

    let req = req_builder.build()?;
    let resp = configuration.client.execute(req).await?;

    let status = resp.status();
    let content_type = resp
        .headers()
        .get("content-type")
        .and_then(|v| v.to_str().ok())
        .unwrap_or("application/octet-stream");
    let content_type = super::ContentType::from(content_type);

    if !status.is_client_error() && !status.is_server_error() {
        let content = resp.text().await?;
        match content_type {
            ContentType::Json => serde_json::from_str(&content).map_err(Error::from),
            ContentType::Text => return Err(Error::from(serde_json::Error::custom("Received `text/plain` content type response that cannot be converted to `models::TabularAnalyzeResponse`"))),
            ContentType::Unsupported(unknown_type) => return Err(Error::from(serde_json::Error::custom(format!("Received `{unknown_type}` content type response that cannot be converted to `models::TabularAnalyzeResponse`")))),
        }
    } else {
        let content = resp.text().await?;
        let entity: Option<AnalyzeGolfCourseTabularError> = serde_json::from_str(&content).ok();
        Err(Error::ResponseError(ResponseContent {
            status,
            content,
            entity,
        }))
    }
}
