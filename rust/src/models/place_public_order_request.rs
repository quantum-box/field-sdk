/*
 * tachyon-field-api
 *
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.16
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PlacePublicOrderRequest : Deliberately tiny.  Where the order goes, which seat serves it, how it is handed over and which channel it belongs to are all resolved from the entry point. `deny_unknown_fields` is what makes that enforceable: a client that tries to add `table_number` gets a 400 instead of being quietly ignored, which is the difference between a contract and a hope.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PlacePublicOrderRequest {
    /// The name called out when the order is ready. Optional, and the only personal data this surface accepts.
    #[serde(
        rename = "customer_name",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub customer_name: Option<Option<String>>,
    #[serde(rename = "items")]
    pub items: Vec<models::PublicOrderLine>,
}

impl PlacePublicOrderRequest {
    /// Deliberately tiny.  Where the order goes, which seat serves it, how it is handed over and which channel it belongs to are all resolved from the entry point. `deny_unknown_fields` is what makes that enforceable: a client that tries to add `table_number` gets a 400 instead of being quietly ignored, which is the difference between a contract and a hope.
    pub fn new(items: Vec<models::PublicOrderLine>) -> PlacePublicOrderRequest {
        PlacePublicOrderRequest {
            customer_name: None,
            items,
        }
    }
}
