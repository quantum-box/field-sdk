/*
 * tachyon-field-api
 *
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.25
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// InvoiceSourceRequest : A record this invoice is being raised from.  `reason` is the caller's own classification. Field stores it verbatim, filters on it exactly, and attaches no meaning to any particular value, so one tenant's `cancellation_fee` and another's `no_show_fee` are the same kind of thing to Field.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct InvoiceSourceRequest {
    #[serde(
        rename = "reason",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub reason: Option<Option<String>>,
    #[serde(rename = "sourceId")]
    pub source_id: String,
    /// One of `reservation`, `order`, `subscription`, `quotation`, `contract`.
    #[serde(rename = "sourceType")]
    pub source_type: String,
}

impl InvoiceSourceRequest {
    /// A record this invoice is being raised from.  `reason` is the caller's own classification. Field stores it verbatim, filters on it exactly, and attaches no meaning to any particular value, so one tenant's `cancellation_fee` and another's `no_show_fee` are the same kind of thing to Field.
    pub fn new(source_id: String, source_type: String) -> InvoiceSourceRequest {
        InvoiceSourceRequest {
            reason: None,
            source_id,
            source_type,
        }
    }
}
