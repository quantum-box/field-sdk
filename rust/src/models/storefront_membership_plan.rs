/*
 * tachyon-field-api
 *
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.13
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// StorefrontMembershipPlan : Deliberately narrower than the staff shape.  `feeJpy` and `validDays` are what a member pays to *join*, which has nothing to do with booking a tee time; publishing them here would put the tenant's membership pricing on a page that only needed a label.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct StorefrontMembershipPlan {
    #[serde(
        rename = "description",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub description: Option<Option<String>>,
    #[serde(rename = "id")]
    pub id: String,
    #[serde(rename = "name")]
    pub name: String,
}

impl StorefrontMembershipPlan {
    /// Deliberately narrower than the staff shape.  `feeJpy` and `validDays` are what a member pays to *join*, which has nothing to do with booking a tee time; publishing them here would put the tenant's membership pricing on a page that only needed a label.
    pub fn new(id: String, name: String) -> StorefrontMembershipPlan {
        StorefrontMembershipPlan {
            description: None,
            id,
            name,
        }
    }
}
