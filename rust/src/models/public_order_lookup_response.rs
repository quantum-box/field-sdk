/*
 * tachyon-field-api
 *
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.25
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// PublicOrderLookupResponse : The order, plus a short-lived handle for reading it again.  The token exists so a confirmation page can refresh without asking for the phone number a second time. It expires, and it is the only thing that opens the by-token read, so it is never put in a URL the caller did not already have to hold.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct PublicOrderLookupResponse {
    #[serde(rename = "expires_at")]
    pub expires_at: String,
    #[serde(rename = "lookup_token")]
    pub lookup_token: String,
    #[serde(rename = "order")]
    pub order: Box<models::PublicOrderResponse>,
}

impl PublicOrderLookupResponse {
    /// The order, plus a short-lived handle for reading it again.  The token exists so a confirmation page can refresh without asking for the phone number a second time. It expires, and it is the only thing that opens the by-token read, so it is never put in a URL the caller did not already have to hold.
    pub fn new(
        expires_at: String,
        lookup_token: String,
        order: models::PublicOrderResponse,
    ) -> PublicOrderLookupResponse {
        PublicOrderLookupResponse {
            expires_at,
            lookup_token,
            order: Box::new(order),
        }
    }
}
