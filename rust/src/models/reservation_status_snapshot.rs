/*
 * tachyon-field-api
 *
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.25
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// ReservationStatusSnapshot : The current state of one reservation, without the booking detail around it.  This is what a caller that already holds reservation ids needs in order to reconcile its own records against Field: whether the reservation is still live, and when it last moved. Keeping it separate from [`Reservation`] is deliberate — the reconciliation contract stays stable even as the booking payload grows, and it carries no customer PII.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct ReservationStatusSnapshot {
    #[serde(
        rename = "cancelledAt",
        default,
        with = "::serde_with::rust::double_option",
        skip_serializing_if = "Option::is_none"
    )]
    pub cancelled_at: Option<Option<String>>,
    #[serde(rename = "id")]
    pub id: String,
    #[serde(rename = "status")]
    pub status: models::ReservationStatus,
    #[serde(rename = "updatedAt")]
    pub updated_at: String,
}

impl ReservationStatusSnapshot {
    /// The current state of one reservation, without the booking detail around it.  This is what a caller that already holds reservation ids needs in order to reconcile its own records against Field: whether the reservation is still live, and when it last moved. Keeping it separate from [`Reservation`] is deliberate — the reconciliation contract stays stable even as the booking payload grows, and it carries no customer PII.
    pub fn new(
        id: String,
        status: models::ReservationStatus,
        updated_at: String,
    ) -> ReservationStatusSnapshot {
        ReservationStatusSnapshot {
            cancelled_at: None,
            id,
            status,
            updated_at,
        }
    }
}
