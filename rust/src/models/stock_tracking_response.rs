/*
 * tachyon-field-api
 *
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.25
 *
 * Generated by: https://openapi-generator.tech
 */

use crate::models;
use serde::{Deserialize, Serialize};

/// StockTrackingResponse : Only fields the ERP context actually holds.  There is deliberately no `productName`, `quantityAvailable`, `quantityAllocated` or `sourceDeliveryId` here: the ERP context stores no product name and models no allocation, and this endpoint is new, so it does not inherit the synthesized values the stock-level list still returns.
#[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
pub struct StockTrackingResponse {
    /// `false` when this request started tracking, `true` when it was already started. The request succeeds either way — it is idempotent.
    #[serde(rename = "alreadyStarted")]
    pub already_started: bool,
    #[serde(rename = "lastUpdatedAt")]
    pub last_updated_at: String,
    #[serde(rename = "quantityOnHand")]
    pub quantity_on_hand: i64,
    #[serde(rename = "sku")]
    pub sku: String,
    #[serde(rename = "stockItemId")]
    pub stock_item_id: String,
    #[serde(rename = "stockLevelId")]
    pub stock_level_id: String,
    #[serde(rename = "warehouseId")]
    pub warehouse_id: String,
    #[serde(rename = "warehouseName")]
    pub warehouse_name: String,
}

impl StockTrackingResponse {
    /// Only fields the ERP context actually holds.  There is deliberately no `productName`, `quantityAvailable`, `quantityAllocated` or `sourceDeliveryId` here: the ERP context stores no product name and models no allocation, and this endpoint is new, so it does not inherit the synthesized values the stock-level list still returns.
    pub fn new(
        already_started: bool,
        last_updated_at: String,
        quantity_on_hand: i64,
        sku: String,
        stock_item_id: String,
        stock_level_id: String,
        warehouse_id: String,
        warehouse_name: String,
    ) -> StockTrackingResponse {
        StockTrackingResponse {
            already_started,
            last_updated_at,
            quantity_on_hand,
            sku,
            stock_item_id,
            stock_level_id,
            warehouse_id,
            warehouse_name,
        }
    }
}
