/* tslint:disable */
/* eslint-disable */
/**
 * tachyon-field-api
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.25
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  ErrorResponse,
  OrderSessionResponse,
  PlacePublicOrderRequest,
  ProductSelectionsResponse,
  PublicOrderEntryPointResponse,
  PublicOrderMenuResponse,
  PublicOrderResponse,
  StartOrderSessionRequest,
  StartOrderSessionResponse,
} from '../models/index';

export interface GetEntryPointRequest {
    publicToken: string;
}

export interface GetMenuRequest {
    categoryId?: string | null;
    search?: string | null;
    limit?: number | null;
    offset?: number | null;
}

export interface GetMenuProductSelectionsRequest {
    productId: string;
}

export interface PlaceOrderRequest {
    idempotencyKey: string;
    placePublicOrderRequest: PlacePublicOrderRequest;
}

export interface StartSessionRequest {
    startOrderSessionRequest: StartOrderSessionRequest;
}

/**
 * 
 */
export class PublicOrderingApi extends runtime.BaseAPI {

    /**
     * Creates request options for getCurrentSession without sending the request
     */
    async getCurrentSessionRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/public/order-sessions/current`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Report the state of the session the caller holds.
     */
    async getCurrentSessionRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<OrderSessionResponse>> {
        const requestOptions = await this.getCurrentSessionRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * Report the state of the session the caller holds.
     */
    async getCurrentSession(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<OrderSessionResponse> {
        const response = await this.getCurrentSessionRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getEntryPoint without sending the request
     */
    async getEntryPointRequestOpts(requestParameters: GetEntryPointRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['publicToken'] == null) {
            throw new runtime.RequiredError(
                'publicToken',
                'Required parameter "publicToken" was null or undefined when calling getEntryPoint().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/public/order-entry-points/{public_token}`;
        urlPath = urlPath.replace(`{${"public_token"}}`, encodeURIComponent(String(requestParameters['publicToken'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Resolve a printed token to the page a diner should see.
     */
    async getEntryPointRaw(requestParameters: GetEntryPointRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PublicOrderEntryPointResponse>> {
        const requestOptions = await this.getEntryPointRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * Resolve a printed token to the page a diner should see.
     */
    async getEntryPoint(requestParameters: GetEntryPointRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PublicOrderEntryPointResponse> {
        const response = await this.getEntryPointRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getMenu without sending the request
     */
    async getMenuRequestOpts(requestParameters: GetMenuRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['categoryId'] != null) {
            queryParameters['category_id'] = requestParameters['categoryId'];
        }

        if (requestParameters['search'] != null) {
            queryParameters['search'] = requestParameters['search'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/public/order-sessions/current/menu`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * The menu this entry point can order from.
     */
    async getMenuRaw(requestParameters: GetMenuRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PublicOrderMenuResponse>> {
        const requestOptions = await this.getMenuRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * The menu this entry point can order from.
     */
    async getMenu(requestParameters: GetMenuRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PublicOrderMenuResponse> {
        const response = await this.getMenuRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getMenuProductSelections without sending the request
     */
    async getMenuProductSelectionsRequestOpts(requestParameters: GetMenuProductSelectionsRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['productId'] == null) {
            throw new runtime.RequiredError(
                'productId',
                'Required parameter "productId" was null or undefined when calling getMenuProductSelections().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/public/order-sessions/current/menu/products/{product_id}/selections`;
        urlPath = urlPath.replace(`{${"product_id"}}`, encodeURIComponent(String(requestParameters['productId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * The variants and options one menu item offers.
     */
    async getMenuProductSelectionsRaw(requestParameters: GetMenuProductSelectionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ProductSelectionsResponse>> {
        const requestOptions = await this.getMenuProductSelectionsRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * The variants and options one menu item offers.
     */
    async getMenuProductSelections(requestParameters: GetMenuProductSelectionsRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ProductSelectionsResponse> {
        const response = await this.getMenuProductSelectionsRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for getOwnOrder without sending the request
     */
    async getOwnOrderRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/public/order-sessions/current/order`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Scoped to the session\'s own order id, so possession of a session is not a way to enumerate the store\'s orders. The response is the same narrow shape the submission returned: no customer contact details come back out.
     * Read back the order this session placed.
     */
    async getOwnOrderRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PublicOrderResponse>> {
        const requestOptions = await this.getOwnOrderRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * Scoped to the session\'s own order id, so possession of a session is not a way to enumerate the store\'s orders. The response is the same narrow shape the submission returned: no customer contact details come back out.
     * Read back the order this session placed.
     */
    async getOwnOrder(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PublicOrderResponse> {
        const response = await this.getOwnOrderRaw(initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for placeOrder without sending the request
     */
    async placeOrderRequestOpts(requestParameters: PlaceOrderRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['idempotencyKey'] == null) {
            throw new runtime.RequiredError(
                'idempotencyKey',
                'Required parameter "idempotencyKey" was null or undefined when calling placeOrder().'
            );
        }

        if (requestParameters['placePublicOrderRequest'] == null) {
            throw new runtime.RequiredError(
                'placePublicOrderRequest',
                'Required parameter "placePublicOrderRequest" was null or undefined when calling placeOrder().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (requestParameters['idempotencyKey'] != null) {
            headerParameters['Idempotency-Key'] = String(requestParameters['idempotencyKey']);
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/public/order-sessions/current/orders`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['placePublicOrderRequest'],
        };
    }

    /**
     * `Idempotency-Key` is required, not optional. A diner taps twice, a phone loses signal mid-request and the app retries, a kiosk reloads: all three arrive here as a second identical POST, and the key is what makes the second one return the first one\'s order instead of creating another.  Failure is handled the same way and for the same reason. When the checkout call does not come back with an answer, the key is marked `failed` and the session is marked `unknown`, which refuses every later attempt on that session rather than risking a duplicate. Staff resolve it; the software does not guess.
     * Place the order this session has been assembling.
     */
    async placeOrderRaw(requestParameters: PlaceOrderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PublicOrderResponse>> {
        const requestOptions = await this.placeOrderRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * `Idempotency-Key` is required, not optional. A diner taps twice, a phone loses signal mid-request and the app retries, a kiosk reloads: all three arrive here as a second identical POST, and the key is what makes the second one return the first one\'s order instead of creating another.  Failure is handled the same way and for the same reason. When the checkout call does not come back with an answer, the key is marked `failed` and the session is marked `unknown`, which refuses every later attempt on that session rather than risking a duplicate. Staff resolve it; the software does not guess.
     * Place the order this session has been assembling.
     */
    async placeOrder(requestParameters: PlaceOrderRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PublicOrderResponse> {
        const response = await this.placeOrderRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for resetSession without sending the request
     */
    async resetSessionRequestOpts(): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/public/order-sessions/current/reset`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * Idempotent on purpose: a kiosk that clears itself after an abandoned order should not have to know whether it still holds a live session.
     * End the session and make its token useless.
     */
    async resetSessionRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const requestOptions = await this.resetSessionRequestOpts();
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Idempotent on purpose: a kiosk that clears itself after an abandoned order should not have to know whether it still holds a live session.
     * End the session and make its token useless.
     */
    async resetSession(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.resetSessionRaw(initOverrides);
    }

    /**
     * Creates request options for startSession without sending the request
     */
    async startSessionRequestOpts(requestParameters: StartSessionRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['startOrderSessionRequest'] == null) {
            throw new runtime.RequiredError(
                'startOrderSessionRequest',
                'Required parameter "startOrderSessionRequest" was null or undefined when calling startSession().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/public/order-sessions`;

        return {
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: requestParameters['startOrderSessionRequest'],
        };
    }

    /**
     * Open a guest session on an entry point.
     */
    async startSessionRaw(requestParameters: StartSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<StartOrderSessionResponse>> {
        const requestOptions = await this.startSessionRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * Open a guest session on an entry point.
     */
    async startSession(requestParameters: StartSessionRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<StartOrderSessionResponse> {
        const response = await this.startSessionRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
