/* tslint:disable */
/* eslint-disable */
/**
 * tachyon-field-api
 * No description provided (generated by Openapi Generator https://github.com/openapitools/openapi-generator)
 *
 * The version of the OpenAPI document: 0.1.25
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  CustomerPurchaseSummaryListResponse,
  CustomerPurchaseSummaryResponse,
  ErrorResponse,
} from '../models/index';

export interface GetCustomerPurchaseSummaryRequest {
    customerId: string;
    from?: string | null;
    to?: string | null;
}

export interface ListCustomerPurchaseSummariesRequest {
    from?: string | null;
    to?: string | null;
    currency?: string | null;
    minPurchaseCount?: number | null;
    dormantForDays?: number | null;
    sort?: string | null;
    order?: string | null;
    limit?: number | null;
    offset?: number | null;
}

/**
 * 
 */
export class TACHYONFieldAnalyticsApi extends runtime.BaseAPI {

    /**
     * Creates request options for getCustomerPurchaseSummary without sending the request
     */
    async getCustomerPurchaseSummaryRequestOpts(requestParameters: GetCustomerPurchaseSummaryRequest): Promise<runtime.RequestOpts> {
        if (requestParameters['customerId'] == null) {
            throw new runtime.RequiredError(
                'customerId',
                'Required parameter "customerId" was null or undefined when calling getCustomerPurchaseSummary().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = requestParameters['to'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/erp/analytics/customer-purchase-summary/{customer_id}`;
        urlPath = urlPath.replace(`{${"customer_id"}}`, encodeURIComponent(String(requestParameters['customerId'])));

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * 1回 = 予約1件。`confirmed` / `completed` で、かつ提供開始 (`startsAt`) が `computedAt` を過ぎたものだけを購入として数える。キャンセル・no-show・ 未提供・未確定は金額にも回数にも入れず、それぞれ件数だけを別項目で返す。 金額は税込で、`reservations.currency` ごとに `currencies` へ分けて返す。 集計はリクエストごとの都度計算で、いつ時点かは `computedAt`。  顧客台帳の存在確認はしない。予約を1件も持たない ID でも 404 ではなく、 すべて 0 のサマリを返す（「購入が無い」は正当な答えなので）。
     * 顧客1件の購買サマリ。
     */
    async getCustomerPurchaseSummaryRaw(requestParameters: GetCustomerPurchaseSummaryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CustomerPurchaseSummaryResponse>> {
        const requestOptions = await this.getCustomerPurchaseSummaryRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * 1回 = 予約1件。`confirmed` / `completed` で、かつ提供開始 (`startsAt`) が `computedAt` を過ぎたものだけを購入として数える。キャンセル・no-show・ 未提供・未確定は金額にも回数にも入れず、それぞれ件数だけを別項目で返す。 金額は税込で、`reservations.currency` ごとに `currencies` へ分けて返す。 集計はリクエストごとの都度計算で、いつ時点かは `computedAt`。  顧客台帳の存在確認はしない。予約を1件も持たない ID でも 404 ではなく、 すべて 0 のサマリを返す（「購入が無い」は正当な答えなので）。
     * 顧客1件の購買サマリ。
     */
    async getCustomerPurchaseSummary(requestParameters: GetCustomerPurchaseSummaryRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CustomerPurchaseSummaryResponse> {
        const response = await this.getCustomerPurchaseSummaryRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Creates request options for listCustomerPurchaseSummaries without sending the request
     */
    async listCustomerPurchaseSummariesRequestOpts(requestParameters: ListCustomerPurchaseSummariesRequest): Promise<runtime.RequestOpts> {
        const queryParameters: any = {};

        if (requestParameters['from'] != null) {
            queryParameters['from'] = requestParameters['from'];
        }

        if (requestParameters['to'] != null) {
            queryParameters['to'] = requestParameters['to'];
        }

        if (requestParameters['currency'] != null) {
            queryParameters['currency'] = requestParameters['currency'];
        }

        if (requestParameters['minPurchaseCount'] != null) {
            queryParameters['minPurchaseCount'] = requestParameters['minPurchaseCount'];
        }

        if (requestParameters['dormantForDays'] != null) {
            queryParameters['dormantForDays'] = requestParameters['dormantForDays'];
        }

        if (requestParameters['sort'] != null) {
            queryParameters['sort'] = requestParameters['sort'];
        }

        if (requestParameters['order'] != null) {
            queryParameters['order'] = requestParameters['order'];
        }

        if (requestParameters['limit'] != null) {
            queryParameters['limit'] = requestParameters['limit'];
        }

        if (requestParameters['offset'] != null) {
            queryParameters['offset'] = requestParameters['offset'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("bearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/erp/analytics/customer-purchase-summary`;

        return {
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        };
    }

    /**
     * 1回の数え方・キャンセル/no-show の扱い・金額未確定・税込は単票と同じ。 金額で並べ替えるため1ページは単一通貨で、`currency` を省略したときに対象へ 通貨が2つ以上あれば 400 にして選ばせる（黙って合算すると呼び出し側が 気づけないまま間違った合計を使う）。  `total` は絞り込み後の顧客数で、`limit` / `offset` とは独立している。 既定では購入が1件も無い顧客は出ない（`minPurchaseCount` の既定が 1）。
     * 顧客ごとの購買サマリを横断で返す。
     */
    async listCustomerPurchaseSummariesRaw(requestParameters: ListCustomerPurchaseSummariesRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<CustomerPurchaseSummaryListResponse>> {
        const requestOptions = await this.listCustomerPurchaseSummariesRequestOpts(requestParameters);
        const response = await this.request(requestOptions, initOverrides);

        return new runtime.JSONApiResponse(response);
    }

    /**
     * 1回の数え方・キャンセル/no-show の扱い・金額未確定・税込は単票と同じ。 金額で並べ替えるため1ページは単一通貨で、`currency` を省略したときに対象へ 通貨が2つ以上あれば 400 にして選ばせる（黙って合算すると呼び出し側が 気づけないまま間違った合計を使う）。  `total` は絞り込み後の顧客数で、`limit` / `offset` とは独立している。 既定では購入が1件も無い顧客は出ない（`minPurchaseCount` の既定が 1）。
     * 顧客ごとの購買サマリを横断で返す。
     */
    async listCustomerPurchaseSummaries(requestParameters: ListCustomerPurchaseSummariesRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<CustomerPurchaseSummaryListResponse> {
        const response = await this.listCustomerPurchaseSummariesRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
